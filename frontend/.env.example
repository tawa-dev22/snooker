# =========================================================================
# SNOOKER LEAGUE & TOURNAMENT MANAGEMENT SYSTEM - FRONTEND ENV TEMPLATE
# =========================================================================

# The API base endpoint pointing to your Express backend (Render instance)
VITE_API_BASE_URL=https://snooker-league-backend.onrender.com

# Enable/Disable real-time stream via Server-Sent Events (SSE)
# Set to 'false' to force the frontend to use TanStack Query fallback smart polling
VITE_ENABLE_SSE=true
