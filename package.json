{
  "name": "snooker-tournament-system-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root package to enable instant monorepo deployment on Vercel and Render",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && mv dist ../dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
